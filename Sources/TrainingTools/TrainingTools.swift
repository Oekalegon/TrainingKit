import TrainingCore

/// Provider-neutral tool registry, JSON schema generation, and the `PlanSandbox` mutation seam
/// that lets an LLM coach propose plan changes without touching the committed stores directly.
public enum TrainingTools {}
